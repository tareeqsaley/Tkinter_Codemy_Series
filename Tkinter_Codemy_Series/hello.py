from tkinter import *

root = Tk() #creates root window

myLabel = Label(root, text = "Hello World!") #creates label widget to be inserted in root window

myLabel.pack() #shove label in first open position on window

root.mainloop()